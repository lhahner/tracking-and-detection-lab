# README Test suit
- Follow this naming convetion when testing a new detector `test_<model_name>_<dataset>_<test-case>.py`
- Everything thats not in `tests/integration` is a unittest.
- Boilderplate code is to some degree allowed, if it provides a clean structure and readability.
